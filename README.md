# Ledger-Live-Local-Logs-Viewer

An app that runs locally (after being initated via CLI) that looks at the logs created in Ledger Live when you select CTR+E.
Written really quickly, use at your own risk! :)
